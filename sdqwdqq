-- [[ UI (Undetected & Assetless Version) ]]

local cloneref = cloneref or function(obj) return obj end

type ConfigType__DARKLUA_TYPE_a = {
    Object: Instance,
    Camera: Instance?,
    Interactive: boolean?,
    Height: number?,
    Focused: boolean,

    Window: any,
    Core: any,
    Tab: any,
    Parent: Instance,
}

local a
a = {
    cache = {},
    load = function(b)
        if not a.cache[b] then
            a.cache[b] = { c = a[b]() }
        end
        return a.cache[b].c
    end
}

do
    function a.a()
        local b

        local d = {
            New = nil,
            Init = nil,
            Shapes = {
                Circle = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 512 },
                CircleOutline = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 512 },
                CircleGlass = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 512 },

                SquircleH = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 325 },
                SquircleHOutline = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 325 },
                SquircleHGlass = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 325 },
                ["SquircleH-TL-TR"] = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 325, AutoChange = false },
                ["SquircleH-BL-BR"] = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 325, AutoChange = false },

                SquircleV = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 325 },
                SquircleVOutline = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 325 },
                SquircleVGlass = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 325 },

                Squircle = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 310 },
                SquircleOutline = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 310 },
                SquircleGlass = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 310 },

                ["Squircle-TL-TR"] = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 310, AutoChange = false },
                ["Squircle-BL-BR"] = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 310, AutoChange = false },
                Square = { Image = "", Rect = Rect.new(0, 0, 0, 0), Radius = 512, AutoChange = false },
            },
        }

        function d.Init(e, f)
            b = f
            return e.New
        end

        function d.New(e, f, g, h, i, j, l)
            local m = {
                Radius = f or 0,
                Type = g or "Circle",
                GetRadius = nil,
                GetType = nil,
                SetRadius = nil,
                SetType = nil,
            }

            local p = {
                ["Glass-0.7"] = "SquircleGlass",
                ["Glass-1"] = "SquircleGlass",
                ["Glass-1.4"] = "SquircleGlass",
                ["Squircle-Outline"] = "SquircleOutline",
            }

            local function GetShape(r)
                return d.Shapes[p[r] or r] or d.Shapes.Circle
            end

            local r = b.New(j and "ImageButton" or "ImageLabel", {
                Image = "",
                ScaleType = l ~= false and "Slice" or nil,
                SliceCenter = m.Type ~= "Squircle" and Rect.new(0, 0, 0, 0) or nil,
                SliceScale = 1,
                ThemeTag = h and h.ThemeTag or nil,
                BackgroundTransparency = 1,
            }, i)

            r = cloneref(r)

            for u, v in next, h do
                if not table.find({ "ThemeTag" }, u) then
                    r[u] = v
                end
            end

            function m.SetRadius(u, v)
                m.Radius = v
                r.SliceScale = math.max(v / GetShape(m.Type).Radius, 0.0001)
                return m
            end

            function m.SetType(u, v)
                m.Type = v
                local x = GetShape(v)
                r.Image = x.Image
                r.SliceCenter = x.Rect
                m:SetRadius(m.Radius)
                return m
            end

            function m.GetRadius(u)
                return m.Radius
            end

            function m.GetType(u)
                return m.Type
            end

            m:SetRadius(f)
            m:SetType(g)

            b.AddSignal(r:GetPropertyChangedSignal("AbsoluteSize"), function()
                local u = GetShape(m.Type)
                if u.AutoChange == false then
                    return
                end

                if string.find(m.Type, "Squircle") then
                    local v = string.find(m.Type, "Glass") and "Glass" or nil
                    local x = string.find(m.Type, "Outline") and "Outline" or nil

                    local z = math.round(r.AbsoluteSize.X / b.UIScale)
                    local A = math.round(r.AbsoluteSize.Y / b.UIScale)

                    local B = m.Radius ~= 0 and m.Radius or math.min(z, A) / 2
                    local C = d.Shapes.Squircle.Radius / 1024
                    local F = B / math.min(z, A)

                    local G

                    if z > A then
                        if F >= C then
                            G = "SquircleH" .. (x or v or "")
                        else
                            G = "Squircle" .. (x or v or "")
                        end
                    elseif z < A then
                        if F >= C then
                            G = "SquircleV" .. (x or v or "")
                        else
                            G = "Squircle" .. (x or v or "")
                        end
                    else
                        if F >= C then
                            G = "Circle" .. (x or v or "")
                        else
                            G = "Squircle" .. (x or v or "")
                        end
                    end

                    if G ~= m:GetType() then
                        m:SetType(G)
                    end
                end
            end)

            return r, m
        end

        return d
    end

    function a.b()
        -- Карта иконок полностью очищена для андетекта
        return {}
    end
end
